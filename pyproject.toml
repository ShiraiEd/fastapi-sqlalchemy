[project]
name = "fast"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Eduardo Egydio Shirai", email = "eduardoegydioshirai599@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "bcrypt<4.1",
    "fastapi>=0.141.1",
    "passlib[bcrypt]>=1.7.4",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
fast = "fast:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
